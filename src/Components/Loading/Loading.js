import React from 'react';

const Loading = () => {
    return (
        <div>
            <button className="btn btn-primary btn-circle text-error loading flex justify-center mx-auto"></button>
        </div>
    );
};

export default Loading;