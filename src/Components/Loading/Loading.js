import React from 'react';

const Loading = () => {
    return (
        <div>
            <button className="btn loading flex justify-center w-24"></button>
        </div>
    );
};

export default Loading;